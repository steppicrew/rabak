
Try the examples:

../../rabak -c rabak.simple.cf conf
../../rabak -c rabak.simple.cf conf example
../../rabak -c rabak.simple.cf backup example

It's also a good idea to use examples as templates for your own configurations.
